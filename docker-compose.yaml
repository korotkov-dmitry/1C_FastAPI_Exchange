version: "3.9"

services:
  backend:
    build: .
    container_name: exchange-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/data_from1c.db
