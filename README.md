# 1С ↔ FastAPI Exchange Backend

Backend-сервис для обмена данными между **1С** и внешней системой  
(на примере FastAPI + SQLite + Docker).

Проект реализован как **pet-project с production-подходом**:
- чёткий контракт API
- идемпотентная загрузка данных
- авторизация
- контейнеризация
- разделение кода и данных

---

## 🧩 Архитектура

```
1С (HTTP) → FastAPI → SQLite
```

- **1С** — источник данных  
- **FastAPI** — слой API, валидации и авторизации  
- **SQLite** — хранилище данных (через Docker volume)

---

## 🚀 Возможности

- 📥 Приём данных из 1С (`POST /api/add_data`)
- 📤 Получение данных (`GET /api/get_data`)
- 🔁 Идемпотентность по `uid` (GUID из 1С)
- 🔐 Авторизация по API-Key
- 📄 Swagger / OpenAPI
- 🐳 Docker + docker-compose
- 💾 Персистентное хранение данных через volume

---

## 🔐 Авторизация

Для интеграции используется **API-Key**.

HTTP-заголовок:
```
X-API-Key: your-secret-key
```

Ключ задаётся через переменные окружения.

---

## 📡 API

### POST `/api/add_data`
Приём и обновление данных из 1С.

**Тело запроса (JSON):**
```json
{
  "uid": "e8f7c7e1-7f6e-4a9a-9d5c-123456789abc",
  "title": "Документ из 1С",
  "done": false
}
```

---

### GET `/api/get_data`
Получение списка данных.

**Ответ:**
```json
[
  {
    "uid": "e8f7c7e1-7f6e-4a9a-9d5c-123456789abc",
    "title": "Документ из 1С",
    "done": false
  }
]
```

---

## 🛠 Технологии

- Python 3.11
- FastAPI
- Pydantic v2
- SQLite
- Docker / docker-compose

---

## 🐳 Запуск проекта

```bash
git clone <repo_url>
cd <repo_name>
mkdir data
docker-compose up --build -d
```

Swagger UI:
```
http://localhost:8000/docs
```

---

## 💾 Хранение данных

Данные хранятся **в Docker volume**, а не внутри контейнера.

```yaml
volumes:
  - ./data:/app/data
```

Каталог `data/` не хранится в git.

---

## 📁 Структура проекта

```
.
├── main.py
├── db.py
├── security.py
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── data/
└── README.md
```

---

## 👤 Автор

Pet-project для развития интеграций **1С + Python**.
